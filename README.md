Image Gallery Web App

A simple, responsive image gallery web application that allows users to upload, view, and delete images. All images are stored locally in the browser using localStorage, which means they remain saved even after refreshing the page.

🚀 Features

Upload images directly from your device

Images are automatically saved in localStorage

Delete any image with a single click

Clean and minimal UI

Fully client‑side (no backend required)

📂 Project Structure
project-folder/
│── index.html
│── style.css
│── script.js
